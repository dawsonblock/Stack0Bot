# Desktop Clients

`apps/desktop` is not an implemented client.

The supported local path does not require a desktop shell. If you experiment with a desktop client such as the included Agent Cowork reference, keep it thin and route model traffic through the same runtime gateway or equivalent provider seam.

This directory is documentation only and is not part of the root build, lint, test, or smoke path.
