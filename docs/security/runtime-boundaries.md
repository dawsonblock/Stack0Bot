# Runtime Boundaries

- GSD and oMLX communicate over HTTP through the gateway.
- no shared hidden state directory is required between them.
- the gateway enforces model allowlisting and optional auth.
- experimental compression paths stay opt-in and benchmark-gated.
